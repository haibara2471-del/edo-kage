# 横版动作游戏 — 讨论稿

**主题定稿：江户时代 · 忍者**
夜晚的江户——屋檐、鸟居、城墙剪影。主角是忍者，核心武器：**手里剑（飞镖）+ 短刀**。

参考：冒险王 / 造梦西游 / 死神vs火影（玩法框架）+ 忍者跑酷类游戏（移动手感）
目标：键盘操作、有动作手感、低计算资源、能快速迭代。

---

## 1. 这类游戏的共性拆解

| 维度 | 说明 |
|---|---|
| 视角 | 2D 横版，角色左右移动 + 跳跃（死神vs火影 还强调空中连段） |
| 操作 | 方向键 + 攻击键 + 技能键，3~6 个键就能玩 |
| 核心手感 | 连招（普攻 1→2→3 段）、受击硬直、击退、打击停顿（hit-stop）、浮空追击 |
| 战斗系统 | 攻击判定框（hitbox）vs 受击判定框（hurtbox），纯矩形检测，极廉价 |
| 敌人 AI | 简单状态机：巡逻 → 发现玩家 → 追击 → 攻击 → 受击 |
| 关卡 | 波次刷怪 / 横版推图，关底 Boss |
| 养成（可选） | 造梦西游式：经验升级、技能点、装备掉落 |

计算量上，同屏 20 个实体每个做 5~10 次矩形相交测试，每帧也就几百次比较——任何现代设备都是零压力。真正的工作量在于**手感调校**和**状态机设计**，不是性能。

## 2. 技术栈候选

### A. 纯 HTML5 Canvas + TypeScript（推荐）
- 零依赖、浏览器直接跑、双击 HTML 就能玩，天然适合"轻量"
- 渲染用 Canvas 2D 画矩形/精灵图，完全够用
- 自己写游戏循环和状态机，代码透明、好调手感
- 代价：没有现成引擎，滚动地图、动画帧管理要自己写（但都不难）

### B. Phaser 3（JS 游戏框架）
- 自带物理、动画、场景管理、资源加载
- 上手快，但框架约定多，调试时隔着一层
- 体积 ~1MB，对这种小游戏略重但可接受

### C. Python + Pygame
- 写起来舒服，但分发麻烦（要打包 exe），且 Pygame 的渲染性能反而不如浏览器 Canvas

**倾向：A**。Flash 游戏的精神续作就该跑在浏览器里。

## 3. 玩法侧重（已结合忍者主题）

推图为主 + 连招爽，忍者主题下具体化为三层乐趣：

1. **移动即乐趣（跑酷感）**：二段跳、空中冲刺（瞬身）、蹬墙跳。屋檐之间飞跃、穿过敌阵——移动本身就爽
2. **远近循环（核心战斗）**：手里剑远程消耗 → 拉近距离 → 短刀连招收割
   - 资源循环：**气**——扔飞镖耗气，短刀命中回气 → 逼玩家远近交替，而不是龟在远处biubiubiu
3. **打击感**：短刀三段连 + 第三段击飞 + 空中可追击（忍者挑空连段）

> 注：不做自动奔跑的 runner（那是另一种关卡设计），保持自由移动 + 跑酷技能，与推图框架兼容。

### 战斗设计细节

| 动作 | 键位 | 说明 |
|---|---|---|
| 移动 | A / D | 忍者步速偏快，带一点起步加速 |
| 跳 / 二段跳 | W 或空格 | 二段跳带残影 |
| 瞬身（冲刺） | L | 短距无敌帧位移，可空中用， cooldown ~1s |
| 短刀连击 | J（连按） | 三段：横斩 → 回斩 → 突刺（击飞） |
| 手里剑 | K | 水平直线飞行，耗气，可三连掷（扇形散布留给技能版） |

### 敌人阵容（江户主题）

| 敌人 | 行为 | 定位 |
|---|---|---|
| 足轻（长枪兵） | 慢速追击，正面突刺 | 杂兵，教玩家绕后/跳跃 |
| 武士 | 会格挡正面攻击，需瞬身绕后或跳跃攻击 | 教玩家用机动性 |
| 敌方忍者 | 远处扔手里剑，近身会闪避 | 教玩家用瞬身切入 |
| Boss：剑客大将 | 多段斩、居合突进 | 第一关关底 |

## 4. 第一版（MVP）建议范围（已按忍者主题修订）

- [ ] 固定时间步进游戏循环（60Hz 逻辑，渲染解耦）
- [ ] 主角：移动、**二段跳、瞬身冲刺**（移动手感是第一优先级，跑酷感的根基）
- [ ] 短刀三段普攻（输入缓冲 + 取消窗口）+ 手里剑（直线飞行道具 + 气资源循环）
- [ ] 打击感三件套：hit-stop、击退、屏幕震动 + 冲刺残影
- [ ] 一种小怪（足轻）：追击 + 突刺 + 受击硬直 + 死亡
- [ ] 一条短关卡：江户屋檐夜景（视差滚动剪影），波次刷 3 波怪
- [ ] 血条、气条、伤害数字

武士/敌方忍者/Boss 放到 MVP 之后的迭代。

## 5. 关键技术点（提前想清楚）

- **游戏循环**：固定步进（accumulator），避免帧率波动影响手感
- **角色状态机**：idle / run / jump / attack1 / attack2 / attack3 / skill / hit / dead，状态间转换规则是手感核心
- **输入缓冲**：按键后缓存 100~200ms，落地/收招瞬间能接上连招
- **判定框**：攻击帧才激活 hitbox，与敌人 hurtbox 做 AABB 相交；每个攻击帧数据化（伤害、击退方向、硬直时间）
- **帧数据驱动**：每段攻击 = {启动帧, 判定帧, 收招帧, 可取消窗口}，配表而不是写死

## 6. 已决策（2026-08-02）

1. **技术栈**：纯 Canvas + TypeScript，Vite 做开发服务器（`npm run dev` 即开即玩，构建产物是静态文件随便部署）
2. **玩法**：推图为主 + 连招爽。第一版只做核心推图 + 三段普攻连招 + 打击感，养成（升级/装备）后置到第二版
3. **美术**：先色块做手感。主角深蓝（夜行衣）、足轻红、Boss 深红，手里剑白色旋转小方块，打击特效白色闪帧 + 火花粒子，背景江户夜景三层视差剪影（远山 / 街町 / 近景屋檐）
4. **平台**：纯网页

## 7. 技术方案细化

- **构建**：Vite + TS（严格模式），零游戏引擎依赖
- **目录结构**（预估）：
  ```
  game/
    index.html
    src/
      main.ts          # 入口 + 游戏循环（固定步进）
      input.ts         # 键盘状态 + 输入缓冲
      entity.ts        # 实体基类（位置/速度/碰撞盒）
      player.ts        # 主角状态机 + 帧数据表
      enemy.ts         # 小怪 AI 状态机
      combat.ts        # hitbox/hurtbox 判定、伤害、hit-stop、击退
      effects.ts       # 屏幕震动、打击火花、伤害数字
      stage.ts         # 关卡/波次刷怪
      ui.ts            # 血条等 HUD
  ```
- **操作键位**（初版）：←→ 移动，↑/空格 跳，J 攻击（连按出三段），K 技能（第二版）
- **帧数据示例**（三段普攻）：
  | 段 | 启动 | 判定帧 | 伤害 | 击退 | 可取消窗口 |
  |---|---|---|---|---|---|
  | 1 | 3f | 4-6f | 5 | 小 | 判定后~收招前 |
  | 2 | 4f | 5-8f | 7 | 中 | 同上 |
  | 3 | 6f | 7-10f | 12 | 大（击飞） | 不可取消 |

## 8. 下一步

按第 4 节 MVP 清单开工，顺序建议：游戏循环+输入 → 主角移动/二段跳/瞬身（手感第一优先） → 短刀三段普攻+帧数据 → 手里剑+气循环 → 足轻 AI → 打击感三件套+残影 → 江户夜景视差背景 → 波次关卡+HUD。

**第一版警告**：跑酷技能（二段跳/瞬身/蹬墙跳）调起来比战斗系统更费时间，蹬墙跳建议放到 MVP 之后再加——先验证"瞬身切入 + 三段连 + 手里剑"这个核心循环爽不爽。

## 9. 迭代记录（规则：每次更新迭代都必须整理到本节）

- **v0.1（MVP）**：游戏循环、三段刀帧数据、手里剑+气循环、足轻 AI、打击感三件套、3 波刷怪、HUD
- **v0.2（美术升级）**：程序化多部件角色（忍者：兜帽/护额/红围巾/反手握刀；足轻：阵笠/板甲/长枪蓄力闪白）、日语标题画面「江戸影」+ 掠月剪影演出 + 樱花、庭院夜景（日式建筑群/障子灯/五重塔/鸟居/松树/石灯笼/苔庭飞石/假山平台/可站立的櫓）
- **v0.3（新系统）**：钩索（初版：自动吸附锚点牵引）、飞行敌人（乌鸦/蝙蝠，盘旋→警告→俯冲）、敌人图鉴（B 键，localStorage 持久化）
- **v0.4（绳索 v2 + 和风卡通调亮）**：
  - **绳索重做**（参考忍者突袭奢华版）：I 键向移动前上方 45° 显式抛出飞索（绳头有飞行过程），钩住横梁/平台/锚点后进入**钟摆摆荡**（A/D 泵摆助力），跳跃键松钩保留动量并补一次二段跳；**每次钩住最多支撑 1 秒**，超时自动断开
  - **天堑路段**：地面开两条 210px 深沟，上方锁链吊横梁——必须用绳索摆荡通过，**坠落直接 game over**；沟前立「！」警示木牌；敌人会在沟边停步（被打落则坠亡）
  - **和风卡通调亮**（参考忍者突袭奢华版的明亮和风）：冷月夜 → 黄昏夕阳（大红夕阳+晚霞云），全套调色板提亮；乌鸦/蝙蝠改亮紫色 + 俯冲红色「！」警告标记，解决深色飞行物看不清的问题
  - 工程化：git 仓库、vite `base:'./'`、GitHub Actions 自动部署 GitHub Pages
- **v0.5（绳索一键化 + 图鉴双页）**：
  - **绳索简化**（用户反馈手动泵摆+手动松手太难）：钩住后**自动朝目标侧泵摆**，荡过支点升至**最高点自动松手飞出**（带 15% 速度加成）——按一次 I 即可完成飞跃；A/D 手动加力、W/I 提前松手作为进阶操作保留；横梁加宽至 110、锚点吸附半径 22、判定外扩 8px；绳索临近断裂（<20 帧）闪烁警告
  - **图鉴双页**：「技能」页（常驻可见的操作指南：三连斩/手里剑/瞬身/二段跳/飞索，键位徽标+机制说明）+「敌人」页（原怪物图鉴，命中解锁），A/D 翻页
- **v0.6（区域封锁 + 绳索自动瞄准）**：
  - **造梦西游式波次封锁**：关卡分为三段战区（被两条深沟隔开），每区一波怪，**清不完结界封住右路**；清完进推进阶段过深沟，进入下一区触发下一波
  - **深沟加宽**：210 → 300/360px，每条沟上方吊**两根横梁**（双摆荡点）
  - **绳索自动瞄准**：按 I 自动锁定前上方最近锚点（不再要求对 45° 角度），支撑时限 1s → 1.25s，自动松手速度加成 15% → 25%——"适时按 I 就基本掉不下去"
  - **敌人分布**：战区波次少飞行敌人（第 2/3 区各 1 只）；**深沟守卫**（第 1 沟 1 乌鸦、第 2 沟 1 乌鸦 + 1 蝙蝠）在推进阶段刷出，骚扰摆荡中的玩家
  - 结界表现：紫色封印墙 + 上浮符咒粒子 + 顶部封印札
- **v0.7（扇形飞镖 + 弓箭手）**：
  - **手里剑·扇形三连**：K 键一次掷出上中下三发（±14° 散布），纵向覆盖飞行敌人航线，仍耗气 10
  - **高台弓箭手**：驻守假山的固定炮台（绿轻甲+头巾+竖弓），玩家进入射界（同高度±46px、距离<520）开始拉弓，放箭前闪白警告；箭水平飞行带轻微下坠（远距离可威胁地面）；第 2/3 战区各 1 名；图鉴新增「弓兵」
- **v0.8（绳索 v3：按住即荡）**（用户自定规则，否决了 A/B/C/D 四个讨论方案后的独创设计）：
  - **按住 I 不放** = 自动朝朝向抛索（自动瞄准最近锚点）→ 钩住后持续摆荡，**无时间限制**；荡过支点自动松、10 帧后自动接力下一根——按住不放就能连续飞跃，基本不会掉沟
  - **松开 I** = 立即脱钩自由落体（想下去就松手）
  - **高度调节**：自动泵摆使振幅越荡越大，高点松手自然甩高；想低直接松手下落
  - 移除 1 秒断绳时限、移除绳索断裂闪烁
- **v0.9（摆荡算法修复，模拟器验证）**：v0.8 实测暴露三个 bug，通过物理模拟脚本（`sim/swing-sim.mjs`，node 直接跑）定位并验证修复：
  1. **泵摆改共振式**：原实现是"钩住时定死方向恒推"（只会偏移平衡点，振幅长不起来）；改为**沿当前切向运动方向加速**（荡秋千式发力），振幅每圈自然增长 → "越荡越高"真的成立
  2. **接力黑名单**：松钩后 40 帧内自动瞄准跳过刚松开的锚点——原来永远钩回刚离开的那根梁，看起来就是原地抽风、没有接力
  3. **朝向锁定**：摆荡期间朝向锁定目标侧，不再随速度翻转（方向感混乱的根因）
  - 钩住瞬间给目标侧初速度（KICK 2.5）；模拟验证：钩→荡→松（v≈9)→接力→松（v≈13)→50 帧过 300px 深沟，每次接力净赚高度
- **v0.10（飞索修練場）**：用户反馈正式关卡里练绳索成本太高——新增**专项训练场景**（标题画面按 I 或访问 `?training=1`）：
  - 单屏 8 根横梁双排（下排 5 根练左右、上排 3 根练升高），起点台 + 矮台（下落目标）+ 终点高台鸟居
  - 覆盖四维度操作：右移（按住 I + D）、左移（+ A）、向上（钩更高排/越荡越高）、向下（松 I 自由落体）
  - 掉落不惩罚：原地复活回起点；到达终点显示「修練完了！」，R 返回标题
  - A/D 手动泵摆力 0.22 → 0.35（方向操控感更明确）
- **v0.11（开发者调试）**：`?debug=1` 开启——数字键 1/2/3 传送三个战区（清场+瞬移）、N 秒清当前波（测结界）、G 无敌开关；HUD 右上角显示 DEBUG 状态；修練場与 debug 可叠加使用
- **v0.12（造梦式技能 + 精英怪 + 无头测试台）**：
  - **玩家三技能（江户风命名）**：**昇月斬**（U，耗气 10，拔刀上挑连人带敌升空）、**朧乱舞**（H，耗气 20，空中九段连斩环绕刀光）、**水月の術**（O，耗气 25，缓行水弹，再按 O/命中/到限引爆 AoE 22 伤）。连招：三段重击挑空 → 昇月斬 → 朧乱舞
  - **精英怪（钟馗钩/大力金刚/蛊术作为怪物属性）**：**鉤使い**（中距甩钩把玩家拽过去，解法：瞬身穿/贴脸）、**金剛**（赤膊巨汉 HP80，刚体——普攻无硬直，只有挑空类打得动；砸地 18 伤跳起躲）、**蠱師**（远程抛物线蛊球→毒雾，中毒持续掉血+减速 30%，保持距离，贴脸杀）
  - **无头测试台 `sim/harness.ts`（`npm run test:sim`）**：Node 直接加载游戏真实源码 + 脚本输入自动游玩 + 断言。11 个场景：绳索过沟×2、连段击杀、长枪命中、三技能、金刚刚体、钩拉、毒雾、乌鸦俯冲。**首轮即抓获 5 个真实 bug**（含 v0.9 模拟副本未发现的绳索失效）：
    1. 状态归纳把 launcher/flurry 当场覆盖成 air（技能放出即消失）
    2. 站在地面即可抛索 → 钩住瞬间被地面"接住"断绳死循环（加：空中才可抛索 + 绳长上限保证弧底不沾地）
    3. 摆荡朝向跟随速度翻转 → 接力瞄准反向（加：行进方向锁定 r.dir，回程经过支点不松手）
    4. 昇月斬判定帧晚于角色上升 → 永远打不到地面敌人（判定提前+框加高）
    5. 蛊球射程只有距离的 44% → 永远落在玩家前方空地（按飞行时间反推力度）
  - 图鉴技能页扩为 8 卡（全江户命名）、怪物页扩为 7 卡双行
- **v0.13（摆荡手感重调：慢而稳）**（用户反馈 v0.12 太快太急促、无法操作）：
  - 摆速上限 12 → 7、共振泵摆 0.2 → 0.1、钩住初速 2.5 → 1.5、松手上抛 -4.5 → -3.5
  - **自动松手增加振幅门槛**（RELEASE_MIN_V 3.5）：小振幅不甩人，荡够了才放；W 随时可主动松手
  - 绳头速度保持 14（快于玩家甩出速度，接力才接得上——这是两条独立的手感轴）
  - 测试台新增**节奏断言**：过沟必须 ≥80 帧（防止把速度调回去）；实测：第一荡有力（7.3）后续轻荡接力（~4），过沟 1.8~2.3 秒
- **v0.14（修炼场实测修复，荡绳手感定稿）**（用户指出应验证修炼场而非副本——新增训练场景测试，抓获三个真问题）：
  1. **"最短摆荡时间"弄巧成拙**：挡住的是速度最合适的第一次过底点，被迫多荡一圈后反而以最大速度甩出——已移除
  2. **钩住瞬间速度软着陆**（切向压到 ≤3）：密梁区不再机枪式连跳，每次摆荡都从容开始
  3. **终点台几何 bug**：终点平台悬空于所有摆荡弧线之下（弧底 y450 平台 y250），物理上不可能降落——终点改为右侧地面鸟居台
  - 定稿手感：钩→轻荡（3~5 速）→ 放 → 接，修炼场 8 荡 3.7 秒通关（测试台 12/12 全过）
- **v0.15（飞索交互重做：扔掉 I 键，全手动蜘蛛侠）**（用户定案：自动一切的模式永远"太快"，因为剥夺了节奏决策权）：
  - **空中按 W = 自动抛索**（附近有锚点；没有才二段跳）——跳跃/飞索合一，零瞄准负担
  - **再按 W = 松手飞出**——截止这次、开始下次的位置全由玩家决定，这是"慢"的真正来源
  - 移除：I 键、按住保持、一切形式的自动松手；A/D 泵摆加速（0.25）、共振保活降到 0.04、限速 6
  - 修炼场入口改按 T；图鉴/HUD 提示同步
  - 测试台改用"摆荡机器人"（按 W 决策）验证：12/12，修炼场 7 荡通关、深沟稳定接力
- **v0.16（移除飞索模块，回归跑跳+战斗）**（用户试玩 v0.15 后裁决：飞索无论何种交互都太麻烦，删）：
  - 删除：飞索全部代码（Rope/摆荡/锚点/自动瞄准）、深沟与横梁、警示牌、飞索修練場、深沟守卫
  - 地面连通（0~2750 无间断）；战区封锁机制保留（边界改为内置常量 ZONES）；跳跃回归纯跳/二段跳
  - 包体 65KB → 57.5KB；测试台移除绳索场景，9/9 战斗与技能场景全过
  - **教训记录**：绳索类机制对手感要求极高，6 轮迭代（自动吸附→钟摆→按住→手动 W）均未过用户手感关；未来若要重做，应作为独立小项目单独验证，不进主线
- **v0.17（钟馗钩视觉重做）**（用户反馈钩使像"拿大炮"）：钩爪改弯月镰刀造型、**钩索飞行时从发射者拖出整条锁链**（链节连线）、手持钩加垂链；命中拉拽加强（vx 4.5 → 10 的钟馗式猛拽）
- **v0.18（钩索直线化 + 钩使策略化）**：钩索**不再受重力**（锁链绷直直出，箭矢仍带下坠）；钩使 AI 策略——**仅当周边 280px 内有队友接应才出钩**（把你拽进长矛兵包围圈），孤军时后撤保持距离不白给；解法因此成立：先清杂兵再处理钩使（测试台验证：有队友时被拉位移 295px 直送敌阵）
- **v0.19（「禅」密令）**：科乐美秘籍式开发者无敌——**2 秒内连按 Z-E-N** 切换无敌（不占游戏键位、不会误触、无需 ?debug=1）；触发时金色「禅」（关无敌显「破」）大字闪现，无敌中右上角常驻红印「禅」
- **v0.20（终极 Boss「龙」+ 双钩使护法）**：
  - 第三战区清场后登场：**黄黑连体衣武僧**（李小龙彩蛋，双节棍、「アチョー!」怪叫），HP 200，专属大血条，结界封第三战区两侧
  - 招式全部有前摇有解：**三连踢**（镜像玩家连段）→ 瞬身穿背；**飞踢突进**（下蹲闪白蓄力）→ 跳/穿，落地大硬直；**升龙踢**（对空惩罚）→ 别乱跳；**残像步**（受击 25% 闪避）→ 扇形镖逼位；**短时间三连受击强制残像**（防无限连）
  - **二阶段**（HP<50%）：橙红气场、攻速提升、残像 40%、新增**双节棍横扫**（55px 中距离）
  - 双钩使护法（有 Boss 作"队友"会持续出钩）；图鉴新增「龍」卡；测试台新增 Boss 场景（10/10）
- **v0.21（调试键收归「禅」旗下）**：传送 1/2/3 与清场 N **仅在「禅」模式（Z-E-N 开启无敌）下生效**——ZEN 是开发总开关，普通玩家无法使用调试功能；?debug=1 只负责显示 DEBUG 标记
- **v0.22（数据统计通道 + 回放地基）**（用户指正：种子不用固定，**记录**即可）：
  - **种子化 RNG**（`rng.ts` mulberry32）：每局随机生成种子并记录到上报数据；Boss 残像/飞鸟相位等玩法随机全部走种子——同种子+同输入=逐帧复现
  - **输入缓冲 tick 化**：墙钟 150ms → 逻辑 9 帧（回放可复现，顺手修了掉帧时缓冲窗口不一致的隐患）
  - **输入日志**：每局全程记录 [帧, 动作, 按下/松开]（几 KB），这就是回放原料
  - **上报通道**（`report.ts` + `config.ts`）：每局结束（通关/死亡 1.5s 后）POST {种子, 结果, 波次, 用时, 输入日志, UA} 到 Supabase；未配置 URL/key 时静默关闭
  - 待办：用户创建 Supabase 项目建 `runs` 表后填入配置；回放播放器（?replay=）待 B 层实现
- **v0.23（数据通道上线 + 防污染）**：
  - Supabase `runs` 表接入完成（端到端验证 201）：每局战报（结果/波次/用时/种子/输入日志）自动入库
  - **防废数据**：开「禅」/传送/清场的作弊局**不上报**；战报打 `env`（local/prod）标签便于过滤
  - **push 网络修复**：github.com 主站被限流（22s+丢包）而 api.github.com 正常——repo 级 `http.proxy=127.0.0.1:7890`（Clash）解决；备选方案 SSH over 443（公钥已生成备用）
- **v0.24（回放播放器）**：
  - `?replay=<id>` 或 `?replay=latest`：从 Supabase 拉取战报，**同种子 + ReplayInput 逐帧喂回输入日志** = 完整重放（从标题画面开始，含 hit-stop）；顶部金色横幅显示回放来源；回放局不再上报
  - 读取需 `runs` 表 select 权限（`create policy "public read" on runs for select using (true);`）
  - **确定性经测试台验证**：同一局实时脚本 vs 日志回放，260 帧后玩家 HP/敌人 HP/位置完全一致（测试台 11/11）
  - 数据收集为**全自动**：每局结束自动上报，无需任何手动操作；作弊局（禅/传送）除外
- **v0.25（回放时间轴修复）**：回放开局从"直接进入 play"改为**从标题画面开始**——日志帧是绝对时间轴（含标题停留时长），此前直接进 play 导致敌人提前刷新、输入与敌人错位（人物站桩挨打、流程不符）；现在记录的 J 键在同一绝对帧触发开局，完全对齐
- **v0.26（玩家身份：忍名登记）**：首次进游戏在标题画面**游戏内输入框登记忍名**（≤12 字符，localStorage 持久化免重复输入，按 N 改名）；每局战报带 `name` 字段，多人共玩可按人归类/过滤（表需 `alter table runs add column name text;`）
- **v0.27（RL 环境 + PPO 烟测通过）**：
  - **架构**：Node env server（`sim/env-server.ts`，真实 TS 游戏代码无头跑，localhost HTTP 暴露 reset/step/obs）+ Python gymnasium 客户端 + stable-baselines3 PPO——游戏逻辑单一来源，不重写
  - 观测 42 维（玩家状态 + 最近 3 敌相对位置/血/类型/状态 + 飞行物）、动作 14 离散组合、奖励（伤害交换/击杀/胜负/逼近/时间惩罚）、场景 ashigaru/wave1/boss
  - **烟测结果：36 秒 4 万步，AI 以 100% 胜率学会击杀足轻**（`ppo_ashigaru.zip`）；吞吐 ~2200fps（requests.Session keep-alive 解决 Windows 端口耗尽；MLP 用 CPU 训练）
  - 环境修复记录：conda 频道已配清华镜像；pip 升级 torch 导致环境损坏的教训（本项目一律 conda 装包 + numpy<2）
- **v0.28（RL 通关训练 + 结界逃逸修复）**：
  - **RL 里程碑**：帧跳跃（15Hz 决策）+ 四分项奖励（伤害/combo/击杀/生存只给结果分）+ 空放惩罚后，**waves 场景 100% 胜率**（三波全清 15 杀、剩血 56~74，`ppo_waves_best.zip`）；PPO 迭代振荡用"最佳模型保存"解决
  - Boss 场景首训翻车为"和平主义"（100 血满生存、零输出）→ Boss 战专用权重（伤害×1.5/承伤×0.5/远离惩罚）
  - 管线提速：批量 HTTP 端点（8 env 1 请求）→ 3000fps；瓶颈确认为单核模拟而非通信
  - **结界逃逸 bug 修复**：结界原来只夹玩家不夹怪，弓箭手被击退穿过结界后波次无法清完——现在玩家和怪物都夹在结界内
- **v0.29（弓箭手 v2：追踪箭）**（用户定案）：箭矢发射后**限速率转向追踪玩家**（0.045 rad/帧、速度恒定 6、红头警告色），可走位/瞬身躲；平衡：装填 85→140 帧、瞄准 42 帧带闪白；射界从"同高度"改为"距离 <480 即可见"；图鉴攻略同步；Boss 战 RL 奖励加大（斩杀 100、里程碑 20/50血、伤害 ×2）
- **v0.30（alibaba AI 观战模式）**：忍名输入 `alibaba` → **AI 代打通关**（浏览器 onnxruntime-web 加载 `ppo_waves.onnx`，15Hz 决策与训练一致，观测镜像 sim/env.ts 42 维）；顶部蓝色横幅「AI 代打中 · alibaba」；代打局不上报；按 N 可改名恢复正常游玩
- **v0.31（RL 排雷与飞鸟易用性）**：
  - RL 环境三连 bug 修复：过波奖励写成每步+2（无限农场）、waveIdx 在 spawn 后被归零（永远刷不出第二波）、训练刷怪 ±120 比真局 ±420 更狠（贴身包夹）；修复后训练健康（EV 0.9），waves 模型平均 12 杀（稳过 1、2 波）
  - 工程纪律：奖励改动后**禁止续训**（价值网络失配会空转）；对照实验先行（简单场景验证管线再查环境）
  - **飞鸟易用性**（用户反馈）：乌鸦 HP 12→10、盘旋高度 150→115（进入短刀判定区）、蝙蝠 100→85、俯冲间隔 90+90 → 70+60——两三下解决，不再拖
- **v0.32（RL 斩龙 + Hard 模式 + AI 观赛通道）**：
  - **RL 里程碑**：bossEasy 课程 100% → 完全体 Boss **100% 胜率**（10/10 斩龙、Boss 血清零，`ppo_boss_100.zip` 留档）；waves 锦标赛最优 run3（过 1-2 波）
  - **Hard 模式**：通关普通 Boss 后进「修羅場・真龍」——强化版（300 血/残像 35%），同一格斗场 solo
  - **AI 观赛通道**：`?ai=boss` → 直接 Boss 场 + 斩龙 AI 代打（`ppo_boss.onnx`，30Hz 决策）；`?ai=waves` → 全程代打
  - 奖励设计反思：环境 100% 不等于真局 100%（刷怪散布/混战/结界差异）——真游戏过关能力需按"每个怪的反制项"重做奖励，一次定型
- **v0.33（AI 训练主线：reward 重构 + 规则堵漏）**：
  - **主线定调**：当前所有工程 effort 优先服务"训练出能强势通关的 AI"，副本机制不再扩展
  - **攻击 affordance**：obs 新增 `inBladeReach` / `inShurikenReach` 两个二进制标志，描述"最近敌人是否在当前面向下的刀/手里剑可达几何范围内"——只给信息，不改 reward 的通用性
  - **Reward 重构（非指向性保持）**：刀伤害 `×0.5`、技能/飞镖伤害 `×0.4`、时间惩罚 `-0.01/帧`、击杀 `+2`、combo 多样性 `+0.1`
  - **气经济解耦**：回气 `+0.02/气`，耗气 `-0.1/气`（与命中脱钩，命中奖励由 damage reward 单独给）
  - **Dash CD 堵漏**：瞬身冷却从 45 帧（0.75s）提到 **300 帧（5s）**——原 CD 下 AI 把无敌位移当免费资源 spam，属规则漏洞
  - **乌鸦**：维持现有被动/主动混合行为，不削弱也不加强
  - **课程成果**：ppo_ashigaru 单足轻 100% → ppo_wave1 单敌/群战 100% → ppo_waves 训练中
  - **waves 评估场景补全**：`sim/ai-analyze.ts` 新增 `waves`（完整三波）场景，与训练环境 (`sim/env.ts`) 完全对齐的刷波/推进逻辑
  - **真相**：ppo_waves（800k 训练）在完整三波上 **0/10 全 timeout**，且退化到连 wave1（足轻×3）都 0%——模型已塌缩为"站着不动"；ppo_wave1 仍是当前最佳：单敌/群战 100%、air/wave2 各 50%、waves/boss 0%
  - **根因判断**：episode 从 3000 帧延长到 5400 帧后 value function 失稳（EV 负值），长程回报估计崩塌导致策略放弃输出；同时观测仅有最近 2 敌人，缺乏战场态势，训练配置 `[64,64]/n_steps=256/gamma=0.99` 对长 episode 严重不足
  - **三管齐下修复**：
    1. **观测升级**：42 维不变，用原 padding 填入剩余敌人数、波次、左右威胁、空中威胁数、最近敌距、推进目标距离
    2. **活跃惩罚**：最近 12 步（~0.8s）场上有敌人但无输出，额外 -0.03/步，打破站原地 spam 局部最优
    3. **训练配置升级**：网络 `[256,256]`、`n_steps=1024`、`gamma=0.995`、lr `1e-4`、ent_coef `0.02`
  - **100 万步从 scratch 训 waves 仍塌**：模型只学会单足轻，wave1 及以上全部 timeout/死亡，说明 PPO 从 scratch 直接吃 waves 太难
  - **训练策略升级 1+2+3**：
    - 加 **VecNormalize**（只归一化 reward，obs 已手动归一化）
    - 网络升到 `[512,512]`
    - `n_steps=2048` 完整覆盖 episode
    - `ent_coef=0.05` 提高探索
    - 支持 `EDO_RL_PORT` 环境变量，方便 worktree 并行跑多组实验
  - **开 worktree 验证方案 4**：稀疏奖励（更 AlphaGo 风格），与主支 1+2+3 并行对比
  - **300 万步并行实验结果**：
    - 主支 1+2+3：EV 一度 0.9+ 但 eval episode length 仍是 1350（timeout），ai-analyze 上 waves 0/10 timeout，wave1 0/10 timeout——模型塌缩为 spam 手里剑 + 站着不动
    - 稀疏奖励：EV 后期变负，表现比主支还差，全场景几乎 0%
  - **结论**：纯从 scratch + 当前 dense/sparse reward + PPO 网络/超参调整，**仍无法让模型学会主动移动/接敌**。根因是 reward 让「站原地 spam 手里剑」成为局部最优
  - **Reward 再调**：刀 0.5 不变、技能/手里剑 0.3、耗气惩罚从 -0.1 提到 -0.2/气，逼 AI 必须近战
  - **Reward 再激进**：刀 0.5、技能/手里剑 0.1、回气 +0.1/气、耗气 -0.5/气、击杀 +5——让远程 spam 彻底亏本，近战终结高回报
  - **Reward 减法修复**：被控惩罚删除、承伤惩罚 0.15 → 0.05，解除对近战换血的压制；保留活跃惩罚防摆烂
- **v0.34（RL 随机训练场景）**：
  - 在 `sim/env.ts` 新增 `random` scenario：每 reset 从 `{足轻, 弓兵, 钩使, 乌鸦, 蝙蝠}` 中随机抽 2~5 个，生成方向 ±1、距离 220~600px 随机，种子化 RNG 保证可复现
  - 目的：**用环境多样性打破固定波次过拟合**，逼模型学习通用接敌/对空/走位，奖励仍保持非指向性 7 项不变
  - 结果：100 万步训练后模型仍塌缩为站桩/手里剑 spam，未改善 waves 泛化
- **v0.35（RL reward 根因修复：时间惩罚符号 bug）**：
  - 复盘所有实验一致失败模式（站桩 timeout），发现 `sim/env.ts` 中时间惩罚写成 `- -0.01 * frames`，实际变成 **+0.01/帧的时间奖励**——模型被激励着站原地等 timeout
  - 修复符号并大幅加重：`-0.05 / 帧`；同时刀伤害 reward 0.5→1.0、技能/飞镖 0.1→0.5、击杀 +5→+20，让主动输出/清场成为严格最优
  - 结果：模型不再站桩，转为自杀式进攻——敢打但不会活，单足轻也 0% 胜率
- **v0.36（RL reward  survival 再调）**：
  - 死亡惩罚 -5 → **-20**，承伤惩罚 0.05 → **0.1**，让「杀敌且生存」成为唯一正回报路径
  - 结果：单敌能赢，但多敌/远程场景回归放风筝 timeout——承伤惩罚过重抑制了近战换血
- **v0.37（RL reward 残血惩罚 + Boss 塔构想）**：
  - 承伤惩罚改为**血量越低惩罚越大**：满血 1x、半血 2x、残血 3x，基础系数回到 0.05。允许满血时积极换血，残血时强制怂
  - 100 万步 waves 训练结果：模型仍在主动进攻（无站桩），但无法稳定取胜——单敌 0%、wave1 0%、完整三波 0%、Boss 0%。说明仅调 reward 仍不足以让模型从 scratch 学会完整 waves  combat
  - 用户提出杀戮尖塔式 Boss 塔：清完三波后进塔，每层不同 Boss（第一层 Boss+ 强化龙，后续层可考虑不知火舞/橘右京/宫本武藏等跨 IP 致敬角色），每过一层重置或保留状态待设计
- **v0.38（调试键更新，方便 RL 试玩）**：
  - `?debug=1` 下新增：
    - **P 键**：切换 AI 代打/人工操作（加载 `public/models/ppo_waves.onnx`，方便实时观察训练模型）
    - **Y 键**：回满血和气
  - Z-E-N「禅」模式保持：1/2/3 传送、N 清场、G 无敌
  - 使用调试/AI 代打的局仍标记为作弊，不上报数据
- **v0.39（RL 极简稀疏 reward ablation）**：
  - 受 AlphaGo 启发，把人工 dense reward 全部砍掉，只保留：通关 +10、死亡 -10、timeout -10、造成伤害 +0.001/点
  - 删除：击杀奖励、承伤惩罚、时间惩罚、combo 奖励、气经济、活跃惩罚
  - 目的：验证"reward 是否过复杂导致 PPO 学不出战斗"；让模型仅凭"赢"和"打人"自己发现连招/走位/气管理
  - 训练：waves 100 万步
  - 结果：**单足轻 100% 胜率，但一切群战/多波场景全部失败**；完整三波 0/10 且全部 timeout，平均卡住 5332 帧
  - 结论：纯稀疏 reward 能学会 1v1，但失去时间压力/活跃惩罚后，AI 在多敌压力下直接退回站桩；说明之前的 dense reward 不是"干扰"，而是防止塌缩的必要信号
- **v0.40（RL 迁移学习尝试，失败）**：
  - 方案：用已会 wave1（100%）/wave2（50%）的 `ppo_wave1` 初始化，fine-tune waves 100 万步
  - 预期：给模型战斗先验，突破从 scratch 学不会 waves 的瓶颈
  - 问题：PPO.load 覆盖了学习率，实际使用 3e-4（不是预期的 3e-5）；训练全程 explained_variance 为负（-0.5 ~ -2.8），value function 完全没学会
  - 结果：比 v0.39 更差——wave1 0/10（平均卡住 1474 帧），完整三波 0/10 全部 timeout，Boss 也大量 timeout
  - 结论：**ppo_wave1 不是合适的迁移起点**，高学习率 + 不同的 reward/归一化分布迅速破坏了原有策略；需要重新考虑方向（self-play / 更长期训练 / 不同初始化）
- **v0.41（RL 固定镜像 self-play，失败）**：
  - 方案：新增 `mirror` 场景，主玩家对战由 `ppo_wave1` 固定的镜像对手；reward 用极简胜负 + 伤害
  - 实现：扩展 `World` 支持 mirror `Player`、新增 `PlayerHittable` 让镜像进入敌人列表、env-server 加载 mirror ONNX
  - 训练：mirror 100 万步，训练指标健康（EV 0.4+）
  - 结果：模型在 mirror 场景上学到行为，但**完全不泛化到标准场景**——单足轻 0/10 timeout、wave1 0/10 timeout、完整三波 0/10 timeout、Boss 大量 timeout
  - 结论：固定对手 self-play 让模型过拟合到镜像行为，无法迁移到真实 waves/boss；若要继续 self-play 必须让对手动态进化
- **v0.42（RL 胜利导向 reward + 玩家反馈：回血机制）**：
  - 用户指出人类玩家可轻松打到 Boss，问题应在于 reward 而非游戏难度
  - 调整 reward：击杀 +20→+100、承伤惩罚 0.05→0.1、死亡 -20→-50、通关 +5→+50、timeout -2→-50
  - 保留：输出伤害、时间惩罚、活跃惩罚；删除 combo、气经济等可能干扰的项
  - 目的：让 AI 以"清场+生存+通关"为第一优先级，用巨额胜负信号压倒局部最优
  - 训练：waves 100 万步
  - 结果：训练指标前所未有地健康（EV 0.9+），**完整三波场景平均卡住帧 = 0**——AI 不再站桩，全程主动战斗
  - 但胜率仍为 0%，问题从"不主动"转为"主动但打不赢"
  - **玩家反馈实现**：新增主动技能「血飲」（I 键），消耗 20 气开启 3 秒吸血 buff，期间造成伤害的 70% 回血；Boss 战/ Hard Boss 战开始时回复 20 血和 20 气（非强制满血）
  - **玩家反馈（v0.43 追加）**：吸血比例 30% → **70%**——时长短 3s + CD 8s，30% 偏弱
- **v0.43（RL 时间惩罚符号修复 + 击杀反通胀 + 承伤基础回落）**：
  - 复盘 v0.42 失败：ai-analyze 显示完整三波全部 timeout 且**满血**（playerHp=100），停在 x≈984 结界前；发现 `sim/env.ts:327` 时间惩罚写成 `+0.05 * frames`（每步 +0.2 **存活奖励**），是 v0.35 同款符号 bug——v0.35 修成 `-0.05/帧` 后，v0.42 重写 reward 时复发
  - 该 bug 解释了 v0.42 的"健康指标"：模型被付工资存活，蹲到 timeout 净赚 +180；"EV 0.9+""卡住 0 帧"只是存活收益的镜像，胜率 0% 是必然
  - 修复 + 再平衡（用户确认"试试看"）：
    - `0.05 * frames` → `-0.05 * frames`（真·时间惩罚，-0.2/步）
    - 击杀 +100 → +50（15 敌总量约 750，与伤害总量 ~900 同量级，避免击杀尖峰在 norm_reward 下把 dense 信号压成噪声，让模型能学到躲避/换血）
    - 承伤基础 0.1 → 0.05，**保留与剩余血量反比**（满血1x/半血2x/残血3x）；v0.36 已记录 0.1 抑制近战换血
  - 改后预算：速通 ≈ +1580，蹲 timeout ≈ −320
  - 训练：waves 短训验证 → ai-analyze 对比（期望满血 timeout 消失、击杀数从 0 转正）
  - 结果：待评估

## 10. TODO / backlog

**玩法模式（已出设计方案，待排期实现）**
- [ ] **钟馗钩**（钩爪夺兵）：飞索命中**敌人**时触发拉拽而非钩锚——轻敌（足轻/弓兵/蝙蝠）被直接拉到玩家面前并附加长硬直（白送一套三连）；重敌/Boss 拉不动，反而把玩家拉过去（高风险突进）。耗气 15，与摆荡共用 I 键（索命中谁算谁）。实现点：绳头飞行阶段加敌人矩形检测、新增 'reel' 拉拽状态、敌人加重量属性
- [ ] **大力金刚**（刚体模式）：H 键切换「短刀 / 金刚」双姿态——金刚姿态：移速 -40%、不可瞬身，但普攻变两段重拳（启动慢、单发 25/40 伤、**出手带刚体**（不被打断）、大击退+重震屏），手里剑变「掷岩」（单发重抛）。定位：清足轻群和打 Boss 硬直的选择，与忍者高速低伤互补
- [ ] **蛊术**（毒蛊领域）：U 键抛出蛊罐（耗气 20，抛物线），落地生成直径 180px 毒雾 4 秒——雾中敌人中毒：每 0.5s 掉 2 血 + 减速 30%，离开雾后再持续 2 秒。视觉：紫绿粒子+敌人头顶蛊纹。定位：封路/控场，配合结界波次战风筝敌人

**终极 Boss 设计（讨论定稿方向：死神vs火影式，弃拳皇式）**
- [ ] **「龙」（ロン）· 异邦武僧**（李小龙彩蛋：黄黑紧身衣 + 双节棍）：第三战区清场后登场，HP 200，专属大血条
  - 设计原则：我们的角色系统是"前摇-判定-硬直"的帧数据驱动，做不了拳皇式立回博弈，但适合做**死神vs火影式**的"看招拆招"——Boss 每招都有明显前摇和对应解法
  - 招式表（全部有解法）：
    | 招 | 表现 | 玩家解法 |
    |---|---|---|
    | 三连踢 | 镜像玩家的三段连，第三下高踢 | 瞬身穿到背后打收招 |
    | 飞踢突进 | 下蹲蓄力（闪白）后全程突进踢 | 跳过或瞬身穿，落地有大硬直 |
    | 升龙踢 | 对空技，玩家在空中时触发 | 别乱二段跳，勾引后打落地 |
    | 残像步 | 受击瞬间 25% 概率残像闪避（无敌+位移） | 用扇形镖覆盖逼位置，别无脑连 |
  - 二阶段（HP<50%）：攻速+30%、残像概率 40%、新增双节棍中距离横扫
  - 场地：第三战区平地，结界封两侧

**内容扩展**
- [ ] 武士（格挡正面攻击，教绕后/空中攻击）
- [ ] 敌方忍者（远程互射 + 闪避）
- [ ] 蹬墙跳（跳跃系统的自然延伸，替代飞索提供垂直机动）
- [ ] 假山复杂地形（斜坡、多层立体庭院）
- [ ] 养成系统（经验/技能点/装备掉落，造梦西游式）

**工程**
- [ ] 音效（挥刀/命中/手里剑/跳跃，WebAudio 合成或免费素材）
- [ ] 移动端触屏虚拟按键
