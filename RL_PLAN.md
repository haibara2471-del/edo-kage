# 江戸影 AI 迭代体系（迭代-分析-修改-增强）

目标：**AI 平推所有关卡、打过所有 boss**。约束：**拒绝过拟合**，reward 只用非指向性项。

---

## 一、Reward 定义

### 当前实验：胜利导向 Reward（v0.44，训练中）

v0.43 回放确认失败模式（单足轻/对空 100%，但足轻×3 → 边退边镖 timeout；两侧散开 → 边跑边挥刀砍空被夹击，1059 帧死总伤害仅 13）。根因：击杀 +50 尖峰在 norm_reward 下把 dense 信号压成噪声 → 模型只有"敌人死了=大奖励"，没有"贴身终结"梯度。v0.44 重排数值经济：

| 项 | 系数 | 说明 |
|---|---|---|
| 刀伤 | +1.0 / 点 | dense 主力（技能/镖 +0.5/点） |
| **击杀** | **+15**（原 +50） | 击杀尖峰不再压灭 dense；收残敌边际 +15 vs 拖着=时间持续在亏 |
| **承伤** | **-0.03 / 点**（原 -0.05，去残血放大） | 单点承伤本就便宜（挨1点 -0.03 vs 刀1点 +1），残局不再吓退模型 |
| **时间** | **-0.1/帧 = -0.4/步**（原 -0.2/步） | 归一后不再形同虚设，逼终结而非拖延 |
| 活跃 | -0.03/步 | 12 步无输出 |
| 死亡 | -50 | 一次性 |
| 通关 | +50 + 血×0.02 | 一次性 |

### 历史：通用 Reward（v0.34–v0.38，已废弃）

之前尝试过多项通用 reward：输出伤害（刀 1.0 / 技能飞镖 0.5）、击杀 +20、残血承伤惩罚、时间惩罚 -0.05/帧、combo 多样性、气经济、活跃惩罚。成功治好了"站桩 spam"，但模型仍无法从 scratch 打赢 waves。本次实验验证"reward 是否过复杂导致干扰"。

**新增 affordance（非 reward）**：obs 提供 `inBladeReach` / `inShurikenReach` 两个二进制标志，告诉策略"当前面向下最近敌人是否在刀/手里剑几何可达范围内"——只补信息，不打破通用性。

**新增战场态势（非 reward）**：42 维 obs 用原 padding 填入剩余敌人数、当前波次、左右威胁、空中威胁数、最近敌距、推进目标距离——让模型看见"还有多少人、从哪来、要不要移动触发下一波"。

---

## 二、迭代-分析-修改-增强 循环（检测→分析→迭代）

```
┌─────────────────────────────────────────┐
│ 1. 检测 (Detect)                          │
│    跑 ai-analyze.ts，输出结构化报告：      │
│    · 各场景胜率（多 seed 方差）           │
│    · 死亡原因归类（被什么杀死的）         │
│    · 死亡位置/时间、卡住不动的帧          │
│    · 动作分布（是否只用一个动作）         │
├─────────────────────────────────────────┤
│ 2. 分析 (Analyze)                         │
│    从报告找"失败模式"：                   │
│    不是"胜率低"，而是"被钩使拉死 4/10"      │
│    判断：是环境问题（该怪没练够）          │
│         还是奖励问题（某能力信号缺失）      │
├─────────────────────────────────────────┤
│ 3. 迭代 (Iterate)                         │
│    环境问题 → 环境课程补课（出那个怪）      │
│    奖励问题 → 慎重评估是否加通用项（极少）   │
│    PPO 续训 + 锦标赛多模型选优            │
├─────────────────────────────────────────┤
│ 4. 回到 1，直到全场景通过                 │
└─────────────────────────────────────────┘
```

---

## 三、训练场景难度课程

环境自己决定 AI 在哪练（reward 不知道在哪）：

```
stage 0  ashigaru（单足轻，入门）
stage 1  ashigaru ×3（1vN 基础）
stage 2  足轻×2 + 弓兵 + 钩使（混兵反制）
stage 3  足轻×3 + 乌鸦×2（空防）
stage 4  足轻×2 + 弓兵 + 钩使 + 蛊师（真局 wave2 难度）
stage 5  足轻×3 + 乌鸦 + 弓兵 + 钩使 + 蛊师（真局 wave2）
stage 6  Boss 单（单挑节奏）
stage 7  Boss + 双钩使（真 Boss 战）

random   随机战斗（2~5 个敌人，类型/方向/距离随机）——训练通用战斗，防止过拟合固定波次
```

- 固定 stage 用于评估，暴露失败模式。
- `random` 用于训练：每 reset 从 `{足轻, 弓兵, 钩使, 乌鸦, 蝙蝠}` 中随机抽 2~5 个，位置在玩家 ±220~600px 随机，朝向 ±1 随机。模型必须在非稳态分布里学通用接敌/对空/走位。
- 每个 stage 通过标准：10 局评估 ≥80% 胜率且死亡原因不集中。从 AI 当前卡住的 stage 继续。

---

## 四、分析工具（ai-analyze.ts）

每轮迭代前必跑，输出 JSON + 文本报告：
- 按 stage 跑 N 局（多 seed），记录：
  - 每局：是否胜利、通关帧数、玩家死亡帧、死亡时最近敌人类型、玩家 HP 曲线
  - 汇总：胜率、通关时长中位数、死亡原因 Top3、卡住帧占比
- 文本打印 + 存 `sim/analysis/<stage>_<timestamp>.json`

---

## 五、防过拟合约束

1. **Reward 只有 6 个通用项**（见第一节），每项都回答"加强通用能力"而不是"过拟合某敌人"。
2. **多 seed 评估**（每 stage ≥3 个 seed），只看方差后的真实胜率，不看单局神迹。
3. **失败模式归类驱动迭代**：不答"怎么赢更多"，只答"被什么杀死的最多"——环境针对性补课，reward 不动。
4. **冻结纪律**：任何 reward 系数改动，必须新开 run 且备份旧模型。
5. **诚实评估**：训练环境 100% 不等于真局 100%。每级通过标准必须用**接近真局的场景构成**测，不能用课程里的简化场景自欺欺人。

---

## 六、当前状态与下一步

- [x] 实现 `sim/ai-analyze.ts`（分析工具）
- [x] env.ts 奖励替换为非指向性 7 项（含技能溢价、combo 多样性、气经济）
- [x] 观测加入敌人速度/面向与攻击 affordance
- [x] 跑 stage 0-1，验证管线健康
- [x] bossEasy → Boss 完全体 100% 胜率
- [x] 用 1+2+3（VecNormalize + [512,512] + n_steps=2048 + ent=0.05）重新训练 waves 300 万步
- [x] worktree 并行验证方案 4：稀疏奖励
- [x] 用 `ai-analyze.ts` 对比两组模型
- [x] 添加 `random` 随机训练场景并跑 100 万步
- [x] 残血承伤惩罚非线性化
- [x] 极简稀疏 reward（胜负 + 造成伤害）ablation 训练 100 万步
- [x] ppo_wave1 迁移 fine-tune waves（失败）
- [x] 固定镜像 self-play（失败）
- [x] v0.42 胜利导向 reward 训练 100 万步
- [x] v0.43 时间惩罚符号修复 + 课程纪律恢复（训练=random 随机课程 / 测试=waves 固定）
- [ ] v0.44 击杀+50→15 / 承伤0.05→0.03 / 时间-0.2→-0.4每步，300k random 训练验证中

---

## 七、实验结论与下一步

### 已验证

1. **站桩根因**：时间惩罚符号 bug 修复后，站桩问题消失。
2. **reward 调参上限（旧）**：重赏输出/击杀、HP 残血惩罚、极简稀疏 reward 都未能让模型从 scratch 打赢 waves。
3. **纯稀疏 reward 的缺陷**：没有时间惩罚/活跃惩罚时，AI 在 1v1 能赢，但在群战中直接退回站桩 timeout。
4. **迁移学习失败**：ppo_wave1 初始化 + fine-tune 未能保留战斗先验。
5. **固定镜像 self-play 失败**：模型过拟合到固定镜像对手，不泛化到标准 waves/boss 场景。
6. **用户反馈**：人类玩家可轻松打到 Boss，问题应在于 reward 设计。
7. **v0.42 胜利导向 reward 突破**：训练指标前所未有健康（EV 0.9+），**完整三波场景卡住帧 = 0**——AI 不再站桩，全程主动战斗。但胜率仍为 0%，问题从"不主动"转为"主动但打不赢"。
8. **玩家反馈实现**：新增主动吸血技能「血飲」（I 键），Boss/Hard Boss 战开始时回复 20 血/气（非满血）。
9. **v0.43 失败模式（回放确认，非猜）**：单足轻/对空 100%，但足轻×3 同侧→边退边镖 timeout（镖 spam + 发呆，6 血残敌收不掉）；两侧散开（真局站位）→ 边跑边挥刀刀刀砍空，1059 帧死、总伤害仅 13。根因定位在 **reward 数值失衡**：击杀 +50 尖峰把 dense 信号压成噪声，模型只有"敌人死了=大奖励"，没有贴身终结的梯度。

### 当前方向

v0.44（击杀 50→15 / 承伤 0.05→0.03 / 时间 -0.2→-0.4/步）300k random 课程训练中。训完**先看回放**（ai-replay.ts 的 both/wave1 场景）再跑 ai-analyze，对照 v0.43 检查：
- 足轻×3 同侧是否还边退边镖（收尾梯度是否建立）
- 两侧散开是否还无脑换血（时间压力是否逼出分割战场）
- waves 测试集第一波能否活过 1059 帧

根据实际数据决定下一步 reward/观测/训练调整。
